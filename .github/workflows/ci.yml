name: ci

on:
  push:
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m py_compile airflow/dags/*.py
      - run: pip install "dbt-postgres==1.10.2"
      - run: dbt deps --project-dir dbt --profiles-dir dbt
